# Reference implementation (cloned separately, read-only spec)
reference/

# Build artifacts
build/
*.o
*.a
*.so
*.wasm
core/tests/bin/

# Web (dist/ is rebuilt by web/build.sh, incl. by the Pages workflow;
# web/weights/ IS committed: the static site serves the policy from there)
web/node_modules/
web/dist/

# Python
__pycache__/
*.pyc
.venv/

# OS/editor
.DS_Store
traces/
